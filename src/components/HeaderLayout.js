import logo from "../images/netflixlogo.svg";

// functional component for the logo layout in the header
function HeaderLayout() {
  return <img src={logo} width={148} height={44} alt="Logo" />;
}
export default HeaderLayout;
